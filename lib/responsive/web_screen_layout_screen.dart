import 'package:flutter/material.dart';

class WebScreenLayoutScreen extends StatelessWidget {
  const WebScreenLayoutScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
